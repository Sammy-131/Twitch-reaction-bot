run = "bash -lc 'python3 -m pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8000'"

# Use a Python Replit with the requirements file. This ensures dependencies are installed before the server starts.
